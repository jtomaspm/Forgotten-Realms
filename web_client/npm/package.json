{
  "name": "npm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf target",
    "build_css": "npx @tailwindcss/cli -i ./src/css/styles.css -o ./target/css/styles.css",
    "build_ts_debug": "npx tsc",
    "build_ts": "npx tsc --declaration false --declarationMap false --sourceMap false",
    "build": "npm run clean && npm run build_ts && npm run build_css", 
    "build_debug": "npm run clean && npm run build_ts_debug && npm run build_css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
